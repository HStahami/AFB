{
  "name": "afb-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "python scripts/sync_backend.py",
    "build": "python scripts/sync_backend.py && cd frontend && npm install && npm run build"
  }
}
